/// Représente un département d'enseignement (ex: "Sciences", "Lettres").

class Departement {
  final String id;
  final String nom;

  Departement({
    required this.id,
    required this.nom,
  });
}
